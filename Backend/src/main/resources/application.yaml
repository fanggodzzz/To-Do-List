spring:
  datasource:
    url: jdbc:mysql://localhost:3306/to_do_list_db
    username: pro
    password: helloworld123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

logging:
  file:
    name: logs/application.log
  level:
    root: INFO
